.container
  %header.row
    .col-md-12
      %span.logo Qiita Copy
  .row
    .col-md-8.col-md-offset-2
      .welcome
        %h1 Qiita Copy へようこそ!
        %p 新規登録して利用を開始しましょう。
      = simple_form_for(:user, url: registration_path(:user)) do |f|
        = f.input :username, label: "ユーザー名"
        = f.input :email, label: "メールアドレス"
        - if @user.password_required?
          = f.input :password, placeholder: "パスワード", label: false
        = f.submit "登録", class: "btn btn-primary btn-block btn-lg"
