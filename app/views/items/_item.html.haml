.row.item-box
  .col-md-12
    .fields
      Author:
      = render item.author
      #{"Tag".pluralize(item.tags.size)}:
      = render item.tags
      .pull-right
        %span.glyphicon.glyphicon-folder-open
        = item.stockers_count
        ・
        %span.glyphicon.glyphicon-comment
        = item.comments_count
        = stock_link_of item
    .title
      = link_to item.title, [item.author, item]

